{
  "name": "highlitecore",
  "productName": "HighliteCore",
  "packageManager": "yarn@4.9.1",
  "version": "1.0.2",
  "private": true,
  "main": "src/index.ts",
  "devDependencies": {
    "esbuild": "^0.25.4"
  },
  "scripts": {
    "core:dist": "yarn esbuild src/index.ts --bundle --minify --sourcemap --outfile=dist/highliteCore.js"
  },
  "dependencies": {
    "idb": "^8.0.3"
  }
}
